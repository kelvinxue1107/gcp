apiVersion: v1
kind: pod
metadata:
  name: kuard
spec:
  containers:
    - image: gcr.io/kuar-demo/kuard-amd64:blue
      name: kuard-test
      ports:
        - containerPort: 8080
          protocol: tcp
          name: http
